results

Auto-generated by the six `model_*.ipynb` notebooks in `src/` — don't edit these by hand.

Each notebook writes:
- `<model_name>_predictions.csv` — that model's predictions over the shared test period
- `model_comparison.csv` — one row per model (RMSE, MAE, MAPE), re-written in place each time a notebook runs, so this file accumulates all six models' scores as people finish theirs
